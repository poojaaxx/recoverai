package com.recoverai.domain;

/** How time-sensitive an AI recovery recommendation (Phase 5) considers itself to be. Advisory only - never a safety signal. */
public enum Urgency {
    LOW,
    MEDIUM,
    HIGH
}
